{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "auth",
  "description": "Capability for the auth window",
  "windows": ["auth"],
  "permissions": ["core:event:allow-emit"],
  "remote": {
    "urls": ["http://localhost:5173/*", "https://*.hydralauncher.gg/*"]
  }
}
